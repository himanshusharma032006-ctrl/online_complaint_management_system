{
  "name": "college-complaint-management-system",
  "version": "1.0.0",
  "description": "Online College Complaint Management System",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "setup": "npm install && cd server && npm install && cd ../client && npm install && cd ../server && node database/seed.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "npm run install:all && cd client && npm run build && node ../build-post.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
